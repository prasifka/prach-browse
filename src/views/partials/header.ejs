<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/styles.css">
  <link rel="icon" href="/img/favicon.ico">
</head>
<body>
  <header class="prach-header">
    <div class="prach-header-content">
      <div class="prach-logo-small">
        <a href="/">Prach Browse</a>
      </div>
      <div class="prach-nav">
        <a href="/settings">Settings</a>
        <a href="/about">About</a>
      </div>
    </div>
    <% if (typeof url !== 'undefined' && url) { %>
      <form action="/browse" method="GET" class="prach-header-form">
        <input type="text" name="url" value="<%= url %>" placeholder="Enter URL or search query" />
        <button type="submit">Go</button>
      </form>
    <% } %>
  </header>
  <main class="prach-main">
